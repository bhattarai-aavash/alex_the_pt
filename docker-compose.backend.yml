services:
  backend:
    build:
      context: ./personal_trainer_agent
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    env_file:
      - ./personal_trainer_agent/.env
    command: uvicorn server:app --host 0.0.0.0 --port 8000

